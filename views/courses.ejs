<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MDCAT Fail courses</title>
<link rel="icon" href="https://i.postimg.cc/cJzdRhcZ/M-pe.png">
</head>
<body style="font-family: 'Arial', sans-serif; margin: 0; padding: 0; background-color: black; color: white;">

      <!-- Navigation Menu -->
      <%- include('partials/header') %>

    <!-- Logo -->
    <header>
        <img id="logo" src="https://i.postimg.cc/cJzdRhcZ/M-pe.png" alt="Logo" style="width: 90%; max-width: 500px; margin-left:35%">

    </header>

    <!-- Course Listing Section -->
    <section style="padding: 30px; text-align: center;">
        <h1 style="color: #cb9f5e;">Our Courses</h1>
        <p style="font-size: 1.2em;">Choose a course, pay the fee, and start learning today!</p>
    </section>
<section>
<div style="display: flex; flex-wrap: wrap; justify-content: space-between; gap: 20px;">

    <% if (courses && courses.length > 0) { %>
        <% courses.forEach(function(course) { %>
            <div style="flex: 1 1 calc(33.333% - 20px); max-width: calc(33.333% - 20px); border: 1px solid #ddd; border-radius: 8px; overflow: hidden; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); font-family: Arial, sans-serif; background-color:#222;">
                <img src="<%= course.image %>" alt="Course Image" style="width: 100%; height: auto;">
                <div style="padding: 15px;">
                    <h2 style="margin: 0 0 10px; font-size: 20px; color: #cb9f5e;"><%= course.title %></h2>
                    <p style="margin: 0 0 18px; font-size: 14px; color: white"><%= course.description %></p>
                    <p style="margin: 0 0 15px; font-size: 14px; color: white">Fee: $<%= course.fee %></p>
                    <button style="display: block; width: 100%; padding: 10px; font-size: 16px; background-color: #007bff; border: none; border-radius: 4px; cursor: pointer;">
                        <% if (user) { %>
                            <form action="/enroll" method="POST">
                                <input type="hidden" name="courseId" value="<%= course._id %>">
                                <button type="submit" style="background:none; border:none; color: white; cursor: pointer; font-size: 16px;">Enroll Now</button>
                            </form>
                        <% } else { %>
                            <a href="/auth/login" style="color: #fff; text-decoration: none;">Enroll Now</a>
                        <% } %>
                    </button>
                </div>
            </div>
        <% }); %>
    <% } else { %>
        <p>No courses available at the moment.</p>
    <% } %>

</div>

</section>
<!-- Footer Section -->
<%- include('partials/footer') %>

<script>
// JavaScript for MDCAT Courses Page

// Function to handle navigation highlight on scroll
document.addEventListener('scroll', () => {
    const sections = document.querySelectorAll('section');
    const navLinks = document.querySelectorAll('nav a');

    let currentSection = '';

    sections.forEach((section) => {
        const sectionTop = section.offsetTop - 50;
        if (window.scrollY >= sectionTop) {
            currentSection = section.getAttribute('id');
        }
    });

    navLinks.forEach((link) => {
        link.classList.remove('active');
        if (link.getAttribute('href').includes(currentSection)) {
            link.classList.add('active');
        }
    });
});
</script>
</body>
</html>
